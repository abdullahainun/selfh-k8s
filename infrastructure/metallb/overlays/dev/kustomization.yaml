apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: metallb-dev

# Use the base metallb
resources:
  - ../../base

# Development-specific patches (if needed)
# patchesStrategicMerge:
#   - configmap-patch.yaml