apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Infrastructure deployment order with proper dependencies
# Deploy in sequence: core → databases → monitoring

resources:
  - core.yaml
  - databases.yaml
  - monitoring.yaml