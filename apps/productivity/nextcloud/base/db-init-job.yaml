# Database initialization job for Nextcloud
# Creates database and user in PostgreSQL
apiVersion: batch/v1
kind: Job
metadata:
  name: nextcloud-db-init
  namespace: nextcloud
spec:
  template:
    spec:
      restartPolicy: OnFailure
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: db-init
        image: postgres:16-alpine
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          runAsNonRoot: true
          runAsUser: 999
          runAsGroup: 999
          seccompProfile:
            type: RuntimeDefault
        env:
        - name: PGHOST
          value: "postgresql.postgresql.svc.cluster.local"
        - name: PGPORT
          value: "5432"
        - name: PGUSER
          value: "postgres"
        - name: PGPASSWORD
          value: "password123"
        command:
        - /bin/sh
        - -c
        - |
          set -e
          echo "Creating Nextcloud database and user..."
          
          # Create database
          createdb -h $PGHOST -p $PGPORT -U $PGUSER nextcloud || echo "Database already exists"
          
          # Create user and grant permissions
          psql -h $PGHOST -p $PGPORT -U $PGUSER -d postgres << EOF
          DO \$\$
          BEGIN
            IF NOT EXISTS (SELECT FROM pg_catalog.pg_user WHERE usename = 'nextcloud') THEN
              CREATE USER nextcloud WITH PASSWORD 'nc-secure-password-2024';
            END IF;
          END
          \$\$;
          
          GRANT ALL PRIVILEGES ON DATABASE nextcloud TO nextcloud;
          
          \c nextcloud
          GRANT ALL ON SCHEMA public TO nextcloud;
          GRANT CREATE ON SCHEMA public TO nextcloud;
          EOF
          
          echo "Database setup completed successfully!"